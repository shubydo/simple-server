# Binaries for programs and plugins
*.exe~
*.exe
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
vendor/

# Binary files
# Ignore binary files that have same name sas directories
# e.g. /vendor/github.com/golang/protobuf/protoc-gen-go
# Ignore binary files that are not go files?
server
bin
simple-server

# Editor directories and files
.idea
.vscode
*.swp*
.DS_Store
